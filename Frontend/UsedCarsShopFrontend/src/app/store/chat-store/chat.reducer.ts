// import { createReducer, on } from "@ngrx/store"
// import { setNewChat } from "./chat.action"

// export const initialState:string | null = null
// const _chatReducer = createReducer(
//     initialState,
//     on(setNewChat, (state, { url }) => url || null)   
// )

// export function chatReducer(state: string | null = initialState, action: any){
//     return _chatReducer(state, action)
// }